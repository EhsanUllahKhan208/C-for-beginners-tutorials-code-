namespace v9comments
{
    internal class Program
    {
        static void Main(string[] args)
        {
            int age = 25; // user's age

            // Calculate the remaining years 
            int yearsToRetirement = 65 - age;
           
            /*
  This function calculates the area of a rectangle.
  It takes two parameters: width and height (both doubles).
  It returns the calculated area (also a double).
*/
            double CalculateArea(double width, double height)
            {
                return width * height;
            }

            /// <summary>
            /// This class represents a person with a name and age.
            /// </summary>
public class Person
        {
            /// <summary>
            /// The person's name.
            /// </summary>
            public string Name { get; set; }

            /// <summary>
            /// The person's age.
            /// </summary>
            public int Age { get; set; }
        }

    }
}
}
